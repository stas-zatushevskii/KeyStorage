
1 log in
  1 compare passwords
  2 create t
  3 save in database
  4 return new t to user
  ------------> login successful

2 every request check GetJWTToken
  IF GetJWTToken expired
   - return err code 401 (go to refresh tokens)

3 refresh tokens
  IF Revoke is true
   - go to log in
  ELSE  
   1 create a new pair of GetJWTToken and Refresh t
   2 save in database refresh tokens, and his ttl
   3 return GetJWTToken tokens to user

